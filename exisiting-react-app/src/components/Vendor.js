import React, { Component } from 'react';

class Vendor extends Component {
    render() {
        return(
            <div className="text-primary">Vendor works</div>
        );
    }
}

export default Vendor;
