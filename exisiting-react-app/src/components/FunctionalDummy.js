import React from 'react';

const FunctionalDummy = (props) => {
    return (
        <div className="container">
            <p>${props.name}</p>
        </div>
    )
}

export default FunctionalDummy;
